# 使用供应商中立的原生工具调用循环

AutoLava 的 Agent 采用一个自主推理 Agent，并通过“消息、工具调用、工具结果、继续或结束”的供应商中立协议运行。该设计取代固定 `TurnPlan`、单次批量 `EvidencePlan` 和浅层固定流程，因为数据依赖的调查、分析假设与故障恢复需要动态组合经营工具；权限、门店范围、业务计算、证据真实性和资源上限仍由后端强制执行，只有评估证明单 Agent 不足时才考虑拆分规划、取证或回答子 Agent。
