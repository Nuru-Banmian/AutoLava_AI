# 营业记录状态纯文字与发布设计

> 状态：待用户复核
> 日期：2026-07-18

## 目标

营业记录中的状态不再显示前置圆点，仅显示“营业”“休息”“天气停业”或“未录入”文字。完成验证后，将成果保留在改名后的 `test-used` 分支、本地合并至 `main`、创建 PR，并更新临时服务器。

## 界面范围

- 桌面营业记录表格的“状态”列移除状态文字前的装饰圆点。
- 右侧营业记录详情卡片的“营业状态”字段移除状态文字前的装饰圆点。
- 移动端列表当前已使用纯文字状态，保持不变。
- 状态值、颜色体系、选择行为、详情内容和无障碍名称均不改变。

## 实现与测试

- 从 `RecordTable` 和 `RecordDetailPanel` 中删除仅用于显示圆点的 `aria-hidden` 元素，不使用 CSS 隐藏残留结构。
- 更新两个组件的测试，断言真实状态文字仍正常显示，并且状态容器中不再存在装饰圆点元素。
- 运行直接相关测试和前端生产构建；合并、PR 与部署前运行前端全量测试。

## Git 交付

1. 在当前 `feature/phase-1-foundation` 分支完成并提交变更。
2. 将本地分支改名为 `test-used`，保留对应工作区。
3. 将 `test-used` 本地合并到 `main`。
4. 推送 `test-used` 至 `origin`，创建以 `main` 为目标分支的 PR。
5. 不删除 `test-used` 分支或工作区；不改动工作区内与本任务无关的未提交文件。

## 服务器更新

- 目标为 `root@116.62.112.245` 的 `/opt/autolava`。
- 发布内容来自完成验证并本地合并后的提交，不上传本机密钥、虚拟环境、依赖目录或 Git 元数据。
- 保留服务器 `/opt/autolava/.env`、数据库命名卷和备份目录；不重建或清空业务数据。
- 使用现有 `compose.yaml` 与 `compose.temporary.yaml` 重新构建并启动服务。
- 部署后检查 Compose 服务状态、健康接口及公网 Web 访问；任一步骤失败时停止并报告，不执行破坏性回滚。

## 验收标准

- 桌面表格和右侧详情中的营业状态前均无圆点。
- 状态文字及未录入占位显示正确，移动端行为无变化。
- 前端测试与生产构建通过。
- 本地 `main` 包含交付提交，`test-used` 分支仍存在且已推送，PR 已创建。
- 服务器容器运行正常，健康检查与网页访问成功，原有配置和数据保持不变。
