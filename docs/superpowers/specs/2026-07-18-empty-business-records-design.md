# 营业记录空日期展示设计

> 状态：待用户复核
> 日期：2026-07-18

营业记录以当前筛选范围内的日期为主轴展示。某日没有 `StoreDailyRecord` 时，前端生成仅用于展示的空行，日期正常显示，营业额、营业状态、天气和其他业务字段为空，并标注“未录入”。

不向数据库写入空记录；总营业额、已记录天数和日均营业额只使用真实记录。管理员与普通用户均可看见空日期，但归档门店仍遵循既有只读权限。
